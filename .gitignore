/target
Cargo.lock

# Test data may contain confidential or licensed PSS/E case files — never commit them.
tests/data/external/
# Golden output RPF files are regenerated by `cargo test` — no need to track them.
tests/golden/*.rpf
